function loadData(data) {
    try {
        //load in variables from main process
        console.log("Data loaded:", data);
    } catch (error) {
        console.error("Error loading data:", error);
    }
}

//take variables from submission form and derive additional variables needed for simulation
function deriveVariables() {
    //
}

//now we render basic graphics
function renderGraphics() {
    //
}

